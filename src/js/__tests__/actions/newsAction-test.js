import axios from 'axios';
import * as newsAction from '../../actions/newsAction';
import Dispatcher from '../../dispatcher';

describe('Dispatches an event', () => {
  it('should load articles ', () => { });
});
